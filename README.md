## Project Structure

- `backend/` – PHP 8.3 backend (custom MVC, Composer autoload)
- `frontend/` – Frontend application
- `database/` – SQL schema and seed files
- `docker-compose.yml` – Local development environment

## Run locally
```bash
docker compose up
